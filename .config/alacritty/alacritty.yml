import:
 # - ~/.config/alacritty/themes/themes/falcon.yaml
  - ~/.cache/wal/colors-alacirtty.yml

key_bindings:
  - { key: T,   mods: Control|Shift, action: SpawnNewInstance }

window:
  opacity: 0.8

decorations: buttonless
